/*import { defineConfig } from "vite";

export default defineConfig({
    base: "./",
    build: {
        minify: "terser",
    },
})*/

import { defineConfig } from "vite";

export default defineConfig({
    base: '/2D-portfolio/',
    build: {
        minify: "terser",
    },
})